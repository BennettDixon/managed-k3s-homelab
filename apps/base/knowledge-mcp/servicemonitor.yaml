apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: knowledge-mcp
  namespace: knowledge-mcp
  labels:
    # Without this label the default-valued kube-prometheus-stack silently
    # ignores the monitor (spec §9 — the twice-hit trap).
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: knowledge-mcp
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
